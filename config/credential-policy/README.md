# Credential policy configuration

Provider types, required credential legs, tier floors, and freshness windows are not fully defined
in the blueprint. Do not infer them from identifier formats or demo fixtures.
